{
  "name": "cli-blog",
  "version": "1.0.0",
  "description": "Terminal-style portfolio blog built with Eleventy and React",
  "main": "src/index.html",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy && node scripts/inject-env.js",
    "build:cloudflare": "eleventy && node scripts/inject-env.js",
    "dev": "eleventy --serve --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "blog",
    "portfolio",
    "terminal",
    "eleventy",
    "react"
  ],
  "author": "Tim",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "@google/genai": "^1.13.0",
    "gray-matter": "^4.0.3"
  }
}
