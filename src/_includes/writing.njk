<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- SEO Meta Tags for this specific article -->
    <title>{{ title }} | DuckSquack</title>
    <meta name="description" content="{{ content | truncate(160) | striptags }}">
    <meta name="author" content="Tim Hughes">
    <link rel="canonical" href="https://ducksquack.com/writings/{{ page.fileSlug }}/">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="article">
    <meta property="og:url" content="https://ducksquack.com/writings/{{ page.fileSlug }}/">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ content | truncate(160) | striptags }}">
    <meta property="og:image" content="https://ducksquack.com/og-image.png">
    <meta property="og:site_name" content="DuckSquack">
    <meta property="article:published_time" content="{{ date }}">
    <meta property="article:author" content="Tim Hughes">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:url" content="https://ducksquack.com/writings/{{ page.fileSlug }}/">
    <meta name="twitter:title" content="{{ title }}">
    <meta name="twitter:description" content="{{ content | truncate(160) | striptags }}">
    <meta name="twitter:image" content="https://ducksquack.com/og-image.png">

    <!-- Preconnect for fast font loading -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Google+Sans+Code:wght@400;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Google Sans Code', monospace;
            background-color: #0F172A;
            color: #F8FAFC;
            max-width: 800px;
            margin: 0 auto;
            padding: 40px 20px;
            line-height: 1.7;
        }
        h1 { color: #F25D94; margin-bottom: 0.5rem; }
        .meta { color: #94A3B8; margin-bottom: 2rem; }
        p { margin-bottom: 1.5rem; }
        a { color: #06B6D4; }
        .redirect-notice {
            background: #1E293B;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 2rem;
            border-left: 4px solid #F25D94;
        }
        .redirect-notice a { color: #F25D94; font-weight: bold; }
    </style>

    <!-- Redirect to interactive XP Notepad version -->
    <script>
        // Allow crawlers to see static content, redirect real users to SPA
        if (typeof window !== 'undefined' && !navigator.userAgent.match(/bot|crawler|spider|googlebot|bingbot/i)) {
            // Use replaceState to set the correct URL, then navigate to root
            // The SPA at root will read the URL and show the correct post
            window.location.replace('/#/writings/{{ page.fileSlug }}');
        }
    </script>
</head>
<body>
    <div class="redirect-notice">
        <a href="/">← Back to DuckSquack</a> | Loading interactive version...
    </div>

    <article>
        <h1>{{ title }}</h1>
        <div class="meta">{{ date }}</div>
        {{ content | safe }}
    </article>

    <footer style="margin-top: 3rem; padding-top: 2rem; border-top: 1px solid #334155; color: #94A3B8;">
        <p><a href="/">← Back to DuckSquack</a></p>
    </footer>
</body>
</html>
